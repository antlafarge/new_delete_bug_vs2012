Build and run the app with VS 2012.  
The new is called but the delete not (it should).  

Convert the app to VS 2015 or newer.  
The new and the delete are called.  

This bug has been fixed in VS 2013.  
